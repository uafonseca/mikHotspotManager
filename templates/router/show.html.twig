{% extends 'base.html.twig' %}

{% block title %}Router{% endblock %}

{% block body %}
    <h1>Router</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ router.id }}</td>
            </tr>
            <tr>
                <th>RouterName</th>
                <td>{{ router.routerName }}</td>
            </tr>
            <tr>
                <th>Ip</th>
                <td>{{ router.ip }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ router.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ router.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('router_index') }}">back to list</a>

    <a href="{{ path('router_edit', {'id': router.id}) }}">edit</a>

    {{ include('router/_delete_form.html.twig') }}
{% endblock %}
