{% extends 'base.html.twig' %}

{% block title %}Investmenst{% endblock %}

{% block body %}
    <h1>Investmenst</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ investmenst.id }}</td>
            </tr>
            <tr>
                <th>Mount</th>
                <td>{{ investmenst.mount }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ investmenst.comment }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ investmenst.createdAt ? investmenst.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ investmenst.updatedAt ? investmenst.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('investmenst_index') }}">back to list</a>

    <a href="{{ path('investmenst_edit', {'id': investmenst.id}) }}">edit</a>

    {{ include('investmenst/_delete_form.html.twig') }}
{% endblock %}
